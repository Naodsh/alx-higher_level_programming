#!/usr/bin/node
// Declare a constant array with the three strings
const messages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loop through the array and print each element using console.log
for (const message of messages) {
  console.log(message);
}
