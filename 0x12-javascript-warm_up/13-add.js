#!/usr/bin/node
// Define a function with the name add that takes two parameters
function add (a, b) {
  // Return the sum of the two parameters
  return a + b;
}

// Export the function using the module.exports object
module.exports.add = add;
